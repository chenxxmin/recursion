# Action misslen hierarchy：有效实验结论盘点（持续更新）

> 建立：2026-09-01。**口径**：仅收录 fp32 / fp32+TF32（已实证等价，见
> `AMP_PRECISION_NOTES.md` §2.2）的结果；bf16/16-bit autocast 的结果一律不计入
> （噪声税实证高达 ~50 个点，见同文档 §2.5）。
> 每次相关批次结束后更新本文档并推送。

## 版图速览

```
确定可行：  len1 × N2（99%+，多规模多 seed 稳健）
确定瓶颈：  len1 × N3（断点，67~90%）、len2 本身（天花板 ~96%）、N4→N5（相变崩溃）
悬而未决：  N3+ 放大（fp32 在测）、len2 超大模型（在爬）、N4/N5 放大（未启动）、len4 放大（未启动）
```

## 一、已有确定答案

### 线 A：misslen=1，N 扩展（基线 d256l2，完整 6000 epochs）

| N | miss0.1 | miss0.3 | 判定 |
|---|---|---|---|
| N2 | 99%+（历史 16 组设置全部 ≥92%，多规模多 seed 稳健） | 92.7（最高） | ✅ 可行，无瓶颈 |
| N3 | 86.7 / 89.5 / 84.6 / 66.7（4 seeds：A1×2 + 对照实验×2） | 34.0 / 35.9 | ❌ 瓶颈从这里开始（67~90%，够不到 99%） |
| N4 | 76.1 / 75.2 | 8.1 / 0.9 | ❌ 更差 |
| N5 | 1.1 / 0.9 | 0.9 / 0.9 | ❌ 相变式崩溃（N4→N5 断层） |

**瓶颈定位**：len1 的可行性断在 N3（远早于预期）；N4→N5 之间存在崩溃相变。

### 线 B：misslen=2，N2（历史完整 fp32 数据）

| 规模 | 完整训练最好成绩 |
|---|---|
| d1024l2（0.1） | 96.4 / 93.4（全部历史实验天花板） |
| d256l2 / d256l4 / d512l1 / d1024l1 等 8 组 | 86~94 |

**瓶颈定位**：len2 在 N2 下、所有完整跑完的规模上从未到达 99%，天花板 ~96%——瓶颈是 misslen=2 本身。

### 通用规律

- missprob 0.3 系统性比 0.1 难 20~50 个点，是独立难度轴
- misslen 层级：len1（可达 99）→ len2（天花板 96）→ len4（历史最好 81.6）

## 二、悬而未决（fp32 待测/在测）

| 问题 | 状态（2026-09-01 更新） |
|---|---|
| N3 len1 放大（宽度/深度）能否救回 99%？ | bf16 数据作废；fp32 重测进行中（d512 l4/l6 × 2seeds，GPU 6,7，`experiments/action_misslen1_n3_depth_fp32.json`） |
| len2 N2 在 d1024l4 / d2048 完整训练能否到 99%？ | 进行中：d2048l2 最好 91.2%@1960ep 仍在爬（GPU 0–5，`experiments/action_misslen2_n2_d2048_resume2.json`）；d1024l4 定格 95.0%@1340ep 未续 |
| N4 / N5 len1 在更大规模可行吗？ | 完全未测（任何精度都没扫过放大） |
| len2 的 N3+ | 按决策暂缓（先把 N2 做透） |
| len4 放大 | 只有历史 d256 数据（best 81.6%），未启动 |

## 三、数据来源索引

| 批次 | 目录（/data/cxm/recursion/） | 精度 |
|---|---|---|
| 历史 misslen 矩阵（208 实验） | action_p127_tr64_ood128_misslen/ | fp32 |
| A1 基线 N3/N4/N5 | action_misslen1_n345_baseline/ | fp32 |
| fp32 vs bf16 对照 | action_fp32_vs_bf16_d256l2n3/ | 两组（仅取 fp32 列） |
| d2048 续跑（len2 N2 放大） | action_misslen2_n2_scaleup/ + *_d2048_resume/ + *_resume2/ | fp32（bf16 续跑已废弃） |
| N3 深度扫描 | action_misslen1_n3_depth/ | bf16 ❌ 作废 |
| N3 深度 fp32 重测 | action_misslen1_n3_depth_fp32/ | fp32+TF32（进行中） |

## 更新记录

- 2026-09-01：初版。收录 A1 基线、历史 len2 数据、fp32 vs bf16 对照结论；
  精度讨论归档（见 AMP_PRECISION_NOTES.md）；默认精度回退 fp32+TF32。
